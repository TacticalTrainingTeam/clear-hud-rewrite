# main

Core component. Disables the multiplayer chat window on mission start and adds a CBA keybind (default: `shigt` + `,`) to toggle it back on/off, with a hint showing the current state and bound key.

Depends on CBA and ACE (for the hint display).
